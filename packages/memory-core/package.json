{
  "name": "@mynd-memory/memory-core",
  "version": "0.1.0",
  "description": "Shared agent memory simulation model, seeded profiles, and deterministic demo logic for MyndMemory.",
  "type": "module",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check": "tsc -p tsconfig.json --noEmit",
    "check:fast": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
