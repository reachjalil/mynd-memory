# Harness Resources

Shared skills, prompts, rules, hooks, plugins, agents, and target-level seeds
for generated Codex and Claude surfaces. Add durable agent configuration here,
then run `pnpm harness:validate` and `pnpm harness:preview` before applying it.
